import obj from "./second.mjs";

// exrension with .js must have type module in package.json or use .mjs extension for using import
//.mjs does not require type module.

obj.hello();
console.log(obj.text);
